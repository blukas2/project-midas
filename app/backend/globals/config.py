import os

from backend.globals.location import ROOT_FOLDER

PORTFOLIOS_FOLDER = os.path.join(ROOT_FOLDER, "portfolios")
ASSET_NAMES_FILE = os.path.join(ROOT_FOLDER, "asset_names.json")

CURRENCY = 'EUR'